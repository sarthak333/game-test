import React, { Component } from 'react';
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
    Dimensions
} from 'react-native';

const Buttons = (props) => {
    return (
        <View>
            <Text>
                Button
            </Text>
        </View>
    )
}

export default Buttons;
